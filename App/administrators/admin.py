# Django Import:
from django.contrib import admin

# Application Import:
"""from .models import (
    AdministratorPrevisions,
)

# Register your models here.
@admin.register(AdministratorPrevisions)
class CredentialAdmin(admin.ModelAdmin):
    list_display = (
        'pk', 'name',
    )
    search_fields = (
        'name',
    )"""